"""CLI helpers — small one-shot commands invoked via `python run.py <subcommand>`.

Currently houses `launch.py` (the `open` subcommand for browser-launching
Keepa charts, Amazon listings, supplier searches, and storefront pages
during manual validation). Future CLIs that don't fit inside the engine
strategy YAML model can live here too.
"""
